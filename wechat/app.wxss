/**app.wxss**/
/* 全局样式 */
page {
  background: #f8f9fa;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
}

view {
  box-sizing: border-box;
}

/* 通用按钮样式重置 */
button::after {
  border: none;
}

/* 隐藏滚动条 */
::-webkit-scrollbar {
  display: none;
}
